import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const P = ({text}) => {
  return (

      <Text className="text-base text-gray-300 font-normal">{text}</Text>
  )
}

export default P

const styles = StyleSheet.create({})